# Suggested GitHub setup

Repository name: `intune-diagnostics-studio`

Description: `Local cross-platform log reader for Intune, Entra ID, MDM and IME troubleshooting.`

Suggested topics:

`intune` `entra-id` `microsoft-365` `mdm` `powershell` `endpoint-management` `windows` `hybrid-join`

For GitHub Pages, enable Pages from the `main` branch and root folder. The included `index.html` can then be opened as a small web app.
